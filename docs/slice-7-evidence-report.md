# Slice 7 — MVP release-candidate evidence report

Status: approved MVP release candidate
Verification date: 2026-08-25
Approval date: 2026-08-25
Post-MVP work: not started

## Outcome

Slice 7 passes its product-wide visual, responsive, accessibility and regression completion gate. Authentication, onboarding, Home, Goals, Ask, Benefits and immutable financial-context correction now use one coherent visual language. The same product is intentionally composed for `360 × 800`, `414 × 896`, `768 × 1024` and `1440 × 900` rather than stretching a framed mobile screen.

The approved classification is:

> MVP RELEASE CANDIDATE — APPROVED FOR CONTROLLED DEMONSTRATION AND EVALUATOR REVIEW

Future You is a complete, deterministic and security-tested MVP release candidate, ready for controlled demonstration using the committed local environment and fixtures. It is not approved as a publicly launched, production-scale or live-model-verified service.

No simulator rule, Sarah value, scenario type, classification, goal date, exact-money rule, conversation intent, context-version rule, immutable run, RLS policy, ownership rule, provider boundary or browser/server financial-authority rule changed in Slice 7.

The live OpenAI gate remains separate and unchanged:

> BLOCKED — authorised credential/model configuration unavailable

### Release gates passed

- Deterministic product readiness
- Persistence and security readiness
- UI and responsive readiness
- MVP accessibility readiness
- Local/evaluator demonstration readiness

### Release gates blocked or disabled

- Public self-service registration: DISABLED in the approved local Supabase configuration
- Live OpenAI model acceptance: BLOCKED — authorised credential/model configuration unavailable

These statuses are intentionally separate from the passed release-candidate gates.

## Implementation and changed-file summary

Slice 7 adds no migration, table, API contract, simulator operation, provider dependency or financial scenario type.

Production changes are grouped as follows:

- `src/app/globals.css`: consolidated palette, spacing, radii, elevation, type, target-size, layout, safe-area and motion tokens; responsive product/auth/onboarding layouts; shared state and focus styling.
- `src/app/welcome/page.tsx`, `src/app/signup/page.tsx`, `src/app/login/page.tsx`: reference-aligned authentication entry surfaces outside the returning-user shell.
- `src/ui/auth/auth-frame.tsx`, `src/ui/auth/signup-form.tsx`, `src/ui/auth/login-form.tsx`: shared semantic auth frame, password controls, pending states, accessible validation and sanitised errors. Company ID remains absent.
- `src/ui/product-shell/product-shell.tsx`, `src/ui/product-shell/modal-sheet.tsx`, `src/ui/product-shell/surface-state.tsx`: responsive navigation, skip link, one focusable main landmark, accessible sheet focus management, and consistent loading/error states.
- `src/ui/features/ask/ask-conversation-shell.tsx`: shared modal sheet, resilient composer/result spacing and unchanged server-authoritative conversation rendering.
- `src/ui/features/onboarding/manual-onboarding-flow.tsx` and `src/app/settings/financial-context/page.tsx`: coherent step/review styling and explicit immutable-correction framing without changing field or confirmation contracts.
- `src/ui/features/product-surfaces/home-surface.tsx`, `goals-surface.tsx` and `benefits-surface.tsx`: headings, wrapping, state hierarchy and responsive composition refinements only.
- `tests/e2e/helpers/auth.ts`, `tests/e2e/slice-6-product-surfaces.spec.ts` and `tests/e2e/slice-7-release-candidate.spec.ts`: cold-local-Auth resilience, preserved Slice 6 evidence and five complete Slice 7 release tests.
- `tests/e2e/slice-7-release-candidate.spec.ts-snapshots/`: seven explicitly approved visual-regression baseline PNGs.
- `artifacts/slice-7-visual/`: 26 release evidence PNGs.
- `docs/mvp-release-candidate-slice-7.md`: canonical audit, design/responsive/accessibility contract and operational process.
- `docs/slice-7-evidence-report.md`: this evidence and readiness report.
- `docs/future-you-evolution-details.md`: append-only Slice 7 decisions and outcome.

Earlier Slice 5 and Slice 6 screenshot evidence was regenerated by the preserved tests against the final shared UI. The existing Slice 6 evidence/test refinements remain preserved. No supplied mockup image or font is shipped in the production client.

## Design-system report

The root design system now has one source for the approved deep blue `#004AAD`, supporting blue `#5271FF`, cyan `#38B6FF`, purple `#8C5BEA`, pink `#DE53AD`, neutral/status colours, 4 px-derived spacing, fields/cards/sheets radii, elevations, fluid type, 44 px targets, content constraints, safe areas and reduced-motion behavior.

Shared behavior is concentrated where it improves consistency: auth framing, the product shell/navigation, context and state language, and a single modal-sheet primitive. Financially specialised result and onboarding components remain explicit so visual reuse cannot conceal authority boundaries.

The supplied SVGs informed geometry, proportion, navigation and gradient construction. PNGs informed colour impression and hierarchy. Intentional differences are semantic and product-driven: Sarah replaces prototype identities, Company ID is removed, unsupported prompts remain absent, frozen canonical values replace prototype amounts, and no unverified benefit becomes a numerical opportunity. Pixel-perfect reproduction was not claimed or measured.

## Route-by-route visual and responsive report

| Surface | Release-candidate result |
|---|---|
| Welcome | A centred brand introduction and clear Login/Register choice derived from Screen 1. |
| Login | Screen 2 blue-corner composition, visible labels, autofill-ready fields, password control, pending state and sanitised errors. |
| Signup | Screen 3 structure without Company ID; exact password confirmation and accessible client/provider failure treatment. |
| Onboarding | Shared tokens, readable grouping, stable controls and a clearer deterministic preview/confirmation hierarchy. |
| Home | Screen 4–5 decision-first hero, prompt rail and current-path cards; no giant balance, unsupported prompt or inferred benefit. |
| Goals | Screen 6 card hierarchy with server-produced progress/dates and written current, hypothetical and historical distinctions. |
| Ask | Screen 7 energy retained across initial, result, alternatives, clarification, unsupported, failure, history and stale states. |
| Benefits | Stronger workplace provenance, confirmed-fact and honest no-data hierarchy; still informational and numerically inert. |
| Context settings | Explicit successor-version wording, preview-before-confirmation and unchanged historical-run relationship. |

At mobile widths, the four-item bottom navigation remains safe-area aware and content/composer spacing clears it. Prompt rails remain visibly scrollable. At tablet and desktop widths, navigation moves to a persistent left rail, Home/Goals/Benefits use balanced columns where supported, and Ask stays a controlled central column. No breakpoint changes data or capability.

All four routes were checked at all four accepted viewports with no horizontal page overflow and exactly one logical `h1` per settled state.

## Visual evidence and regression

The user explicitly approved generation/update of the Slice 7 Playwright baseline PNGs on 2026-08-25. Seven deterministic baselines now cover:

1. Login
2. Onboarding review
3. Home current path
4. Goals current path
5. Ask initial
6. Ask £650 result
7. Benefits canonical

After generation and review, the definitive full suite ran without `--update-snapshots`; all seven comparisons matched.

`artifacts/slice-7-visual/` contains 26 evidence captures:

- 18 primary mobile captures covering Welcome, Login, Signup, onboarding intro/review, Home upper/lower, Goals current/hypothetical, Ask initial/result/alternatives/clarification/unsupported/provider error, Benefits canonical/no-data and context-correction preview;
- eight wider captures: Home, Goals, Ask and Benefits at both tablet and desktop sizes.

The current Signup capture was specifically reviewed after correcting clipped decorative artwork/text. The final mobile Login and Signup headings remain fully visible, and the desktop/tablet compositions were reviewed after settled rendering with animations disabled.

## Accessibility evidence

The Slice 7 browser tests prove:

- keyboard access to a first-focus skip link and transfer of focus to the main content;
- a trapped conversation-history sheet, logical wrap, Escape close and trigger-focus restoration;
- one logical `h1` on every settled product route at every accepted viewport;
- `aria-current` product navigation and a named product-navigation landmark;
- labelled icon controls, form labels, status/error roles and server-produced progress descriptions retained from prior slices;
- reduced-motion loading animation is disabled or reduced to zero duration;
- long unbroken user/goal names and large/negative money display strings do not create page overflow at 360 px;
- the same stressed page remains usable with a 200% root text-size override;
- history, Send and navigation controls meet the 44 × 44 px minimum.

The shared sheet prevents background interaction while open and restores document body state after close. Required status distinctions use text such as Current path, Viewing only and Older version of your financial plan, not colour alone.

Visual contrast and focus visibility were reviewed against the implemented palette and captured states. No dedicated screen-reader application or automated axe scan was available in this run, so this report does not claim a full assistive-technology certification. A VoiceOver/NVDA review remains a sensible release follow-up, not a discovered functional blocker.

## Behavioural-stability proof

- Sarah's current/preferred safety buffer remains £900.
- The canonical £650 path remains £900 → £250, Bills covered, £0 overdraft, restored November 2026, Emergency fund February 2027 and Affordable · Significant trade-off.
- £500 remains £400 after; £400 remains £500 after; October remains £250 after with unchanged frozen goal dates.
- The strengthened new-user release journey manually recreates the canonical context, reaches the deterministic £650 result, views it in Goals, returns to current path, visits Benefits, signs out and signs back in with state preserved.
- A manually entered OniBank association stays unverified and produces an honest no-benefit-data state; it does not fabricate Sarah's 3%/3% pension fact.
- Sarah's confirmed 3% employee/3% employer pension fact remains informational, and the employer amount remains non-spendable.
- Context correction creates V2 while its V1 run remains readable with the original February 2027 result and an older-plan warning.
- Ask explanations, unsupported intent, clarification, provider failure, persistence and scenario selection retain their Slice 5 behavior.
- Home, Goals and Benefits make zero provider calls. The browser remains a renderer of JSON-safe server facts.

## Authentication decision

The release includes polished `/welcome`, `/login` and `/signup` surfaces. The approved local Slice 3 Auth configuration intentionally keeps public registration disabled. Slice 7 therefore proves Signup presentation and deterministic client validation, then uses the committed no-context Alex identity for the authenticated account-to-onboarding release path. It does not weaken global Auth security merely to make a UI test create a new credential.

Public signup is not operational in the approved demonstration environment. A completely new public visitor cannot create a real account through self-service registration.

In an environment where public registration is deliberately enabled, the same Signup provider boundary supports immediate-session routing to onboarding or a neutral email-confirmation state. Raw Supabase errors are never rendered. This environment-specific registration switch remains an operational limitation, not hidden evidence.

## Security and performance evidence

- All prior forced-RLS, narrow-grant, ownership and anon-denial pgTAP tests remain green.
- Cross-user conversations, contexts and stored runs remain non-enumerable in integration/browser tests.
- Private/no-store and same-origin protections remain green; owner/context identity is still server-resolved.
- No administrative credential, analytics, session replay or financial-data logging was introduced.
- No OpenAI/provider type or adapter entered the browser bundle; the production bundle boundary passed.
- Passive Home, Goals and Benefits retain zero provider calls and no model-provider wait.
- Reference images are evidence inputs only and are absent from production routes and bundles.
- The production build generated 19 static/dynamic page entries plus the existing API routes without adding a new data service or cache.

## Clean local reset proof

Every destructive operation was restricted to the repository's local Supabase stack. Immediately before reset, the active URL was `http://127.0.0.1:54321`, `supabase/config.toml` identified project `future-you`, and `npm run db:reset` resolved to `supabase db reset --local`. No hosted/shared project reference or hosted administrative credential was used.

The reset regenerated the committed seed, recreated the database, applied these migrations in order and reseeded Sarah, Alex and the required test identities:

1. `20260824140000_slice_3_authenticated_persistence.sql`
2. `20260824170000_slice_4_manual_onboarding.sql`
3. `20260824210000_slice_5_conversations.sql`

No migration was added for Slice 7. No dashboard action or manual SQL/data modification was required. Stateful database and browser suites began from clean committed fixtures in their required order.

## Exact verification results

All discovered automated tests passed; none were skipped.

| Gate | Discovered | Passed | Failed | Skipped | Result |
|---|---:|---:|---:|---:|---|
| Vitest unit/regression | 26 files / 231 tests | 231 | 0 | 0 | PASS |
| Supabase integration | 3 files / 10 tests | 10 | 0 | 0 | PASS |
| PostgreSQL/pgTAP | 3 files / 123 tests | 123 | 0 | 0 | PASS |
| Conversation evaluation corpus | 1 file / 34 tests | 34 | 0 | 0 | PASS |
| Fake-provider modes | 1 file / 8 tests | 8 | 0 | 0 | PASS |
| Product-surface authority/provider-zero | 4 files / 20 tests | 20 | 0 | 0 | PASS |
| Full Playwright mobile-Chromium project | 22 tests | 22 | 0 | 0 | PASS |
| Slice 7 release/responsive/accessibility cases | 7 tests | 7 | 0 | 0 | PASS |
| Stable visual-regression comparisons | 7 baselines | 7 | 0 | 0 | PASS |
| Required Slice 7 evidence captures | 26 captures | 26 | 0 | 0 | PASS |

The evaluation, fake-provider, product-authority and Slice 7 counts are named subsets of their parent Vitest/Playwright suites and are shown separately because they are explicit release gates. The latest Playwright pass used one Chromium worker and completed in 1.4 minutes. Chromium was the only browser engine required and run; optional Firefox/WebKit smoke was not attempted.

Additional gates:

- TypeScript: PASS, zero errors.
- ESLint: PASS, zero errors.
- Production Next.js build: PASS.
- Client-bundle/dependency boundary: PASS; no server-only simulator/store identifiers found.
- Generated seed/database-type drift: PASS.
- Clean local reset/migration/seed: PASS.
- Coverage: 26/26 files and 230/230 tests; 77.41% statements (1765/2280), 62.28% branches (1034/1660), 82.53% functions (411/498), 79.88% lines (1628/2038).
- `git diff --check`: PASS.
- Live OpenAI evaluation: BLOCKED — authorised credential/model configuration unavailable.

The complete suite required no weakened Slice 1–6 expectation. During release-journey expansion, three fixture-specific assertions were corrected: generated onboarding goal IDs were located by visible goal content, goal dates were asserted within the goal result card rather than its banner, and unverified OniBank data was correctly asserted as no benefit data rather than Sarah's seeded pension fact. These were test-assumption corrections that strengthened the data-authority proof; product code, expected financial outputs and visual baselines were unchanged.

## Release-readiness matrix

| Area | Status | Evidence/limitation |
|---|---|---|
| Deterministic product | READY | Frozen Sarah and all scenario/authority regressions pass. |
| Persistence and security | READY | Clean migrations, forced RLS, isolation, immutability and browser security gates pass. |
| UI/UX | READY | Coherent mobile/tablet/desktop product with 26 reviewed captures and seven stable baselines. |
| MVP accessibility | READY | Keyboard, focus, motion, zoom, text resilience, semantics and target tests pass; no full AT certification claimed. |
| Controlled local/evaluator demonstration | READY | Use Sarah, Alex, committed fixtures and the deterministic fake provider. |
| Public self-service registration | DISABLED | Local Supabase public registration remains disabled; Signup is not operational for new public visitors. |
| Live-provider quality | BLOCKED | Authorised credential/model configuration unavailable; live corpus was not run. |

## Remaining limitations and post-MVP recommendation

- Public registration is disabled in the approved local Auth configuration; deployments must make an explicit registration/confirmation decision.
- Live-model interpretation quality is not verified.
- No authoritative employer-benefit catalogue exists; Benefits correctly remains sparse and informational.
- Only Chromium was run. Manual VoiceOver/NVDA and optional Firefox/WebKit review would increase release confidence.
- Alignment is close and reference-led, not measured pixel-perfect reproduction.
- Global coverage is reported honestly; database/provider adapters are primarily covered through integration boundaries rather than unit percentages.

Recommended post-MVP priorities, outside the approved MVP: first authorise and run the existing live-provider corpus; then perform manual assistive-technology and optional cross-engine review; only after those gates should product discovery choose among employer-data verification or a newly specified simulator capability. No post-MVP item was started in Slice 7.

## Controlled-demonstration operating position

- Use Sarah for the canonical returning-user financial journey.
- Use Alex for the genuine no-context onboarding journey.
- Use the deterministic fake provider unless the authorised live-provider evaluation is separately completed.
- State explicitly that live-model interpretation quality is not approved.
- Keep every financial calculation server-deterministic and Benefits informational/numerically inert.
- Preserve the local-only migration, reset and committed-fixture process.

The repository is frozen at this verified release-candidate state. Any post-MVP work requires a separately approved contract.

## Approved post-release visual refinement

On 2026-08-25, the repeated “Current plan”/“Current plan active” banner was removed from the top of Home, Goals, Ask and Benefits. Current-plan state is now treated as the normal default rather than repeated chrome. Material exceptions remain explicit: hypothetical, historical-plan and stale-conversation warnings were not removed.

The unused `ContextPill` component and its CSS were deleted, desktop grid placement was tightened, and affected renderer tests now assert that the banner is absent. Five approved visual baselines—Home, Goals, Ask initial, Ask £650 result and Benefits—were regenerated. A dedicated regression checks Home, Goals, Ask and Benefits at `414 × 896` and `1440 × 900`, asserting that neither the old banner hook nor its “Current plan active” text is present. The rebuilt Slice 7 suite passes 6/6, followed by a clean local reset and a no-update full Chromium result of 21/21. TypeScript, ESLint and the production build pass.

## Approved generated-profile refinement

The generic profile SVG in the product header was replaced with an AI-generated fictional Sarah portrait. The built-in image-generation tool produced a natural, racially ambiguous young-adult portrait with a pale blue/lavender background and circular-crop-safe framing. The selected image is stored as `public/images/sarah-profile.png`, resized to `512 × 512` (361 KB), and served through Next.js image optimisation.

The financial-context settings link retains its existing accessible name; the nested portrait uses empty alternative text to prevent duplicate announcement. Unit coverage verifies the optimised image URL, link label, empty `alt` and absence of the former profile SVG. A browser regression verifies the visible image, URL, 40px-or-larger rendered dimensions and SVG absence. Home, Goals and Benefits baselines were regenerated. The rebuilt Slice 7 suite passes 7/7 and the clean no-update full Chromium suite passes 22/22; all 231 Vitest tests, TypeScript, ESLint, production build and whitespace checks pass.

## Approved password-icon refinement

The Login and Signup Show/Hide text controls now use the `FiEye` and `FiEyeOff` React components from the `react-icons` package. Each remains a 44px button with dynamic “Show password(s)”/“Hide password(s)” accessible naming, `aria-controls` and `aria-pressed`; the icon itself is hidden from the accessibility tree.

The browser journey toggles Login and both Signup password fields and verifies their `type` attributes. The Login baseline and Signup evidence were regenerated and visually reviewed. The clean full Chromium suite remains 22/22, while 231/231 Vitest tests, TypeScript, ESLint, production build and whitespace checks pass.

## Approved two-layer goal-ring refinement

Goal percentage badges on Home and Goals now use the supplied reference's two-layer treatment: a solid purple inner disc with white percentage text, separated from a thicker outer progress ring. The outer arc starts at the top, has rounded ends and leaves the unfunded portion as a pale-purple track; only 100% closes the ring. Current, compact and stored-hypothetical goal cards share one renderer, while the existing horizontal progress bar remains unchanged.

The white separation was subsequently widened and the complete indicator then enlarged twice so its percentage label has clear breathing room. The final standard ring is `3.75rem` across, the compact ring is `3rem`, and the inner disc occupies 60% of either indicator. The unfunded track uses a 7-unit stroke while the funded arc retains an 11-unit stroke. A browser geometry assertion requires the standard ring to exceed 56px, the funded arc to remain thicker than the track and the inner disc to remain no more than 61% of the full diameter; the visual baseline protects the combined appearance.

The server now supplies a bounded SVG dash pattern alongside the existing linear fill, so rounded arc geometry remains renderer-only without deriving financial progress in the browser. Renderer-authority coverage asserts the impossible `1234.5 8765.5` circular sentinel verbatim alongside the `12.345%` linear sentinel. The Home and Goals visual baselines were regenerated and visually reviewed with Sarah's partial 73% and 29% rings. The focused no-update browser run passes 2/2, all 231 Vitest tests pass, and TypeScript, ESLint, production build, client-bundle boundary and whitespace checks pass. No simulator mathematics or financial outcome changed.

## Approved Apple-aligned typography refinement

Future You now applies the stricter Apple iOS/iPad typography baseline consistently across its responsive web experience: inherited/default copy starts at `17px`, and no rendered user-facing text may fall below `11px`. The app deliberately does not shrink to Apple's lower Mac minimum at desktop widths because the same browser experience can move between phone, tablet and desktop contexts. Forty explicit sub-11px declarations were consolidated onto `--fy-type-min: 0.6875rem`; `--fy-type-body: 1.0625rem` establishes the default.

A computed-style Playwright audit checks direct text, form text and generated pseudo-labels—not only source declarations. It proves a body default of at least 17px and an 11px floor across Home, Goals, Ask and Benefits at `360 × 800`, `414 × 896`, `768 × 1024` and `1440 × 900`, plus Login, Signup and onboarding review. All seven approved visual baselines were regenerated and reviewed. After a verified local-only Supabase reset, the complete no-update Slice 7 suite passes 7/7; Vitest passes 231/231, and TypeScript, ESLint, production build, client-bundle boundary and whitespace gates pass. The standard is based on Apple's official platform typography guidance: <https://developer.apple.com/design/human-interface-guidelines/typography>.

## Approved segmented goal-bar refinement

The horizontal goal bars now use the reference design's staccato structure while retaining Future You's existing cyan-to-blue gradient. A repeating light separator divides the funded span into short blocks; the unfunded portion remains the outlined empty track. The immutable server-authored `fill` width continues to control the exact funded extent, so this presentation change introduces no browser calculation or financial behavior.

Home, current Goals and stored-hypothetical Goals share the same segmented fill rule. The browser regression requires both a repeating segmentation layer and the underlying colour-gradient layer, while the existing impossible `12.345%` sentinel continues to protect verbatim width rendering. Home and Goals baselines and their evidence captures were regenerated and visually reviewed; production build, TypeScript, ESLint and the focused browser journey pass.

## Approved action-triangle refinement

Interactive arrow glyphs now use a reusable filled SVG triangle aligned with the supplied reference. Right-facing variants appear in the Home hero, Home decision cards, Ask suggested prompts, conversation-history rows and the inactive Benefits handoff. The Ask submit control uses the same triangle rotated upward. Informational arrows inside deterministic before/after results, goal-date comparisons and explanatory link text were deliberately preserved.

The icon remains hidden from the accessibility tree while every parent button/link retains its existing accessible name, destination and touch target. The returning-Sarah browser regression asserts that the right-facing Home and Ask triangles and upward send triangle render, and that the replaced `→`/`↑` text characters are absent from those controls. The Home, Ask initial and Ask £650-result baselines were explicitly regenerated, visually inspected and then passed in a no-update comparison (1/1). Vitest passes 231/231; TypeScript, ESLint, production build, client-bundle boundary and `git diff --check` pass. No financial, provider, persistence or security contract changed.

## Approved taller progress-bar and end-cap refinement

Horizontal goal-progress tracks are now `0.78rem` high, exactly 1.5 times the previous `0.52rem`. Partial funded spans retain a rounded left edge and use a flat right edge; only an exact server-supplied `100%` fill receives the fully rounded end cap. The staccato separators, cyan-to-blue gradient, outlined remainder and verbatim server width remain unchanged.

A shared `GoalProgressBar` prevents current Home, current Goals and stored hypothetical Goals from diverging. Renderer tests cover partial and complete states, and the mobile browser regression verifies at least 12px computed track height plus a zero-radius partial right edge. Home and Goals baselines were regenerated, visually inspected and then passed in a no-update comparison (1/1). Vitest passes 232/232; TypeScript, ESLint, production build and `git diff --check` pass. No DTO, simulator or financial-authority rule changed.

## Approved Apple-aligned icon-scale refinement

Meaningful interface icons now participate in Future You's Apple-aligned responsive type system. A new `--fy-icon-body` token references the existing 17px body token. Action triangles that previously rendered between 12.8px and 16.8px now render at 17px by default and scale with root text size; their SVG artwork was optically enlarged within the frame. Existing prompt, navigation, benefit, password and goal symbols already exceed that scale and remain unchanged. Interactive control hit regions continue to meet the established 44px target.

The browser audit examines every visible meaningful SVG on Login, Signup, Home, Goals, Ask and Benefits and rejects dimensions below 17px. A dedicated regression proves the Home action triangle renders at least 17×17px normally and at least 34×34px under 200% text scaling. Home, Ask initial and Ask £650-result baselines were regenerated and visually reviewed. The combined no-update icon/visual run passes 2/2; Vitest passes 232/232; TypeScript, ESLint, production build and `git diff --check` pass. No accessible name, route, DTO or financial rule changed.

## Shared wordmark refinement under visual review

Login and Signup no longer introduce the compact angular symbol-and-stacked-copy lockup. A shared renderer now supplies the same `FUTURE YOU AI` wordmark used by the authenticated product header, while the full angular emblem remains on Welcome only. The wordmark and its `AI` suffix now render at the 17px body scale instead of 13.44px and 11px respectively.

Regression coverage verifies the Login back link retains its accessible name, contains the complete wordmark, contains no compact symbol, and renders every wordmark segment at 17px or larger. Login and five signed-in visual baselines were regenerated; the combined no-update visual and wordmark run passes 2/2. Vitest passes 232/232; TypeScript, ESLint and the production build pass. The separate evidence-capture clipping noted in UI-02 remains open and was not hidden inside this branding change.

## Superseding symbol and Ask-only AI refinement

The preceding under-review rule was superseded by the requested final naming treatment. The compact angular symbol now precedes every compact Future You wordmark, including Login, Signup, onboarding/settings and all four signed-in destinations. Compact wordmark text increased from 17px to 20px. Product and auth surfaces show `FutureYou`; only Ask shows `FutureYou AI`. Welcome keeps its full-size symbol treatment.

The shared renderer owns the symbol geometry and optional suffix. The product shell derives `AI` exclusively from `active === "ask"`. Renderer coverage proves a default Product header omits `AI` and an Ask header includes it. Browser coverage verifies Login, Signup, onboarding, Home, Goals, Ask and Benefits use the correct variant, includes viewport-bound geometry checks, and exercises the signed-in rule at `360 × 800`, `414 × 896`, `768 × 1024` and `1440 × 900`. The responsive gate passes 1/1; the final no-update visual, icon and onboarding run passes 3/3; Vitest passes 233/233; TypeScript, ESLint, production build and `git diff --check` pass.

## Supplied logo-artwork refinement

The temporary CSS reconstruction of the angular mark was replaced by the user-supplied SVG, stored locally as `public/images/future-you-logo.svg`. One shared renderer uses the same vector artwork for the full Welcome identity and every compact wordmark without stretching its original `109 × 135` proportions. The image remains decorative because the surrounding brand container or link already supplies the accessible name.

Renderer coverage asserts the local SVG URL. Browser coverage verifies the supplied asset on Welcome, Login, onboarding and the authenticated product shell, while existing wordmark geometry and visual baselines protect its placement across the remaining surfaces. The focused update run and subsequent no-update comparison both pass 4/4; Vitest passes 233/233; TypeScript, ESLint, production build and `git diff --check` pass. No remote asset dependency, financial logic or navigation behaviour was introduced.

## Welcome copy reduction

Welcome now retains only the full Future You identity and the Login/Register actions. The former headline, explanatory sentence and trust-note paragraph were removed together rather than left as empty heading chrome. `AuthFrame` now omits its heading container and `aria-labelledby` when no heading content is supplied, while Login and Signup continue to provide their existing labelled headings.

Unit and browser regressions assert that all three removed strings are absent while the Login and Register actions retain the existing `/login` and `/signup` destinations and the supplied SVG remains present. A dedicated Welcome visual baseline records the simplified composition. Authentication, routing and financial behaviour are unchanged.

The Login/Register label refinement passes its final no-update Welcome comparison 1/1. The full Vitest suite passes 234/234; TypeScript, ESLint, production build and `git diff --check` pass.

## Welcome vertical-rhythm refinement

The Welcome identity and entry actions remain centred, but no longer read as one cramped cluster. Responsive spacing now increases the logo-to-name separation, the identity-to-actions separation and the gap between Login and Register. Clamp bounds preserve the intended rhythm across shorter phones and taller screens.

Browser geometry requires at least 72px between the completed identity block and the action group and at least 16px between the two actions at `414 × 896`. A `360 × 640` compact-height check proves that the brand and actions remain inside the viewport rather than being clipped. The Welcome visual baseline records the revised vertical composition.

The updated Welcome visual passes its final no-update comparison 1/1. The full Vitest suite passes 234/234; TypeScript, ESLint, production build and `git diff --check` pass.

## Auth-logo contrast refinement

Login and Register now place the supplied splash SVG between the compact angular logo and the blue header artwork. The repository copy is sanitised, filled white and layered behind the original coloured logo without changing the rest of the `FutureYou` wordmark. Welcome, onboarding and authenticated product headers deliberately omit this backdrop.

Renderer coverage verifies the local backdrop source, white fill, absence of the original black fill, removal of the source DOCTYPE, and absence of scripts. Browser coverage verifies the SVG is present on both Login and Register and absent from Welcome and the authenticated Home header. Updated auth visual evidence protects the final layering and contrast.

The focused no-update Welcome/auth run passes 2/2 and proves the white backdrop is stacked below the coloured logo. The full Vitest suite passes 235/235; TypeScript, ESLint, production build and `git diff --check` pass.

## Welcome spacing and responsive-name refinement

The Welcome identity-to-actions gap increased again and now scales from a larger short-screen minimum toward a taller-screen maximum. At the canonical `414 × 896` viewport the browser gate requires at least 128px of clear space between the identity and Login/Register group, while the existing `360 × 640` containment gate remains in force.

The full identity name stays stacked as `Future` and `You` through `768px`. At `769px` and wider the explicit line break is hidden, producing the single-line `FutureYou` treatment. Browser checks exercise both adjacent breakpoint widths, verify the two word segments share one vertical position above the breakpoint, and preserve mobile and desktop visual baselines.

The final no-update responsive Welcome comparison passes 1/1. The full Vitest suite passes 235/235; TypeScript, ESLint, production build and `git diff --check` pass.

## Further enlarged auth-logo splash

The white Login/Register backing shape increased again, from approximately `4.5rem × 4.25rem` to `6.25rem × 5.9rem` (about 100×94px at the default root size). The auth lockup moved slightly inward and downward, and its icon-to-copy gap widened, so the larger splash remains inside the rounded card and ends before `FutureYou` begins.

Browser geometry now requires at least a 3.1× artwork-width ratio and 2.9× artwork-height ratio, non-negative left/top card clearance, non-negative wordmark clearance, correct overlap and correct stacking. The Login visual baseline protects the enlarged treatment.

The final no-update Login/Register visual comparison passes 1/1. The full Vitest suite passes 235/235; TypeScript, ESLint, production build and `git diff --check` pass.

## Enlarged auth-logo splash

The white Login/Register splash increased from approximately `3.15rem × 3rem` to `4.5rem × 4.25rem`. The auth-only gap between the symbol and `FutureYou` increased at the same time so the substantially larger backing shape remains visually separate from the white wordmark.

Browser geometry requires the backdrop to be at least 2.2 times the artwork width and 2.1 times its height, remain stacked below the logo, overlap the logo, and stop before the wordmark copy begins. The refreshed Login baseline protects the resulting scale and placement.

The final no-update Login/Register visual comparison passes 1/1. The full Vitest suite passes 235/235; TypeScript, ESLint, production build and `git diff --check` pass.

## Reference-sized Welcome Login action

The Welcome action hierarchy now follows supplied Screen 1 more closely. The primary Login pill grows to the available width up to `20.75rem` and has a `4.25rem` minimum height, while Register remains centred at 85% of that action-group width. These rules are scoped to Welcome and do not enlarge authentication form submission controls.

At `414 × 896`, browser geometry requires Login to be at least 320px wide and 68px tall and at least 15% wider than Register. Mobile and desktop Welcome baselines protect the revised primary/secondary hierarchy.

The final no-update responsive Welcome comparison passes 1/1. The full Vitest suite passes 235/235; TypeScript, ESLint, production build and `git diff --check` pass.
